const text=(value,max,required=false)=>{const v=String(value??'').trim();if(required&&!v)throw new Error('VALIDATION_REQUIRED');if(v.length>max)throw new Error('VALIDATION_TOO_LONG');return v;};
const allowed=(value,list)=>{if(!list.includes(value))throw new Error('VALIDATION_INVALID_OPTION');return value;};
export function inferRegion(postcode=''){const p=String(postcode).toUpperCase().replace(/\s+/g,'');return /^(M|OL|BL|SK)/.test(p)?'greater-manchester':/^(LU|E|EC|N|NW|SE|SW|W|WC)/.test(p)?'london-luton':'';}
export function validateBookingPayload(input,config={}){
  const service=allowed(input.service,Object.keys(config.rates||{}));const time_slot=allowed(input.time_slot,(config.timeSlots||[]).map(x=>x.id));const region=input.region||inferRegion(input.postcode);allowed(region,Object.keys(config.serviceRegions||{}));
  return {service,frequency:text(input.frequency,40,true),property_type:text(input.property_type,60,true),bedrooms:text(input.bedrooms,8,true),bathrooms:text(input.bathrooms,8,true),postcode:text(input.postcode,12,true).toUpperCase(),region,date:text(input.date,10,true),time_slot,addons:Array.isArray(input.addons)?input.addons.slice(0,12).map(x=>text(x,60,true)):[],other_addon:text(input.other_addon,160),first_name:text(input.first_name,80,true),last_name:text(input.last_name,80,true),email:text(input.email,180,true).toLowerCase(),phone:text(input.phone,40,true),address:text(input.address,300,true),notes:text(input.notes,1200),terms:Boolean(input.terms),estimate:Number.isFinite(Number(input.estimate))?Number(input.estimate):null,estimatedHours:Number.isFinite(Number(input.estimatedHours))?Number(input.estimatedHours):null,reference:text(input.reference,40,true)};
}
export function validateFeedbackPayload(input){return{name:text(input.name,60,true),email:text(input.email,180,true).toLowerCase(),phone:text(input.phone,40),rating:Number(allowed(Number(input.rating),[1,2,3,4,5])),feedback:text(input.feedback,500,true),website:text(input.website,120)};}
